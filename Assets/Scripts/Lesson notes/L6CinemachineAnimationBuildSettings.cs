using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class L6CinemachineAnimationBuildSettings : MonoBehaviour
{
    /*
     cinemachine: built in Unity camera control machine 
        attach to camera object
        
    Animation window: record animations using sprites
        assign your selected Sprite to the Sprite field of the SpriteRenderer component at the intended time in the animation timeline
        this will set the keyframes for the animation
        
    Scene: organizational tool used to compartmentalize components of a project so that what the user needs is accessible and performant
    
    build settings: scenes that the user will interact with, platforms for distribution, and other build settings

     */
}
