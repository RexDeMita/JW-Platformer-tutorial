using UnityEngine;

namespace Lesson_notes
{
    public class L14AnimatorFireballParticleSystemSettings : MonoBehaviour
    {
        /*
     animator settings: 
        loop time: bool that sets the animation to loop or not
        
    triggers that trigger an animation
        when used in an event, the set trigger method is the string version because the trigger had a name
        
    particle system settings: 
        collision tab: type, collides with, mode, send collision messages(if there is a script with particle collisions)
        
        shape tab: the shape that will contain the emitted particles
        
     */
    }
}
