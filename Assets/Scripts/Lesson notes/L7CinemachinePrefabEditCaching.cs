using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class L7CinemachinePrefabEditCaching: MonoBehaviour
{
    /*
    CinemachineTargetGroup: set the target(s) of the camera and how the camera follows them
     
    prefab edit mode: edit a prefab so that the changes apply to all the prefabs in the scene
    
    always add checks for colliders
    
    sprites can be animated or changed by swapping out sprites based on collisions
    
    caching: calling methods once in the life of the object instead of once per frame
     */
}
