using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class L5DebugLayersGrid : MonoBehaviour
{
    /*
     debug inspector: allows you see information relevant to debugging like private fields
     
     layers: tool used to separate objects in the scene. It is useful for physics detection and calculation
     
     grid snapping tool: snap objects to a grid. working with objects that are whole numbers in size helps with this snapping
     
     local vs global: parameters will affect objects differently if you are editing parameters relative to the object or the world origin
    
     */
}
